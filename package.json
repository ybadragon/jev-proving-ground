{
  "name": "jev-proving-ground",
  "version": "1.0.0",
  "private": true,
  "description": "Small, self-contained TypeScript modules. Not real software.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/**/*.test.js\""
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5"
  }
}
