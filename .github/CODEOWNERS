# Brian owns everything here. Agents open PRs against this repo autonomously,
# so review is the gate that keeps a machine-authored change from landing
# unseen — including changes written by an agent that was told to plant a
# defect on purpose.

* @ybadragon
